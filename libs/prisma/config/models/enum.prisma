enum UserStatus {
    ACTIVE
    INACTIVE
    WITHDRAWN

    @@schema("public")
}

enum Site {
    ADMIN
    USER

    @@schema("public")
}

enum JwtType {
    ACCESS
    REFRESH

    @@schema("public")
}

enum Platform {
    ANDROID
    IOS
    WEB

    @@schema("public")
}
