enum UserStatus {
    ACTIVE
    INACTIVE
    WITHDRAWN

    @@schema("public")
}

enum Owner {
    ADMIN
    USER

    @@schema("public")
}

enum JwtType {
    ACCESS
    REFRESH

    @@schema("public")
}

enum Platform {
    ANDROID
    IOS
    WEB

    @@schema("public")
}

enum TokenType {
    JWT
    FCM

    @@schema("public")
}
